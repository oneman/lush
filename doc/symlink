Non-proxy virtualized concept recognition via proxy or photographic memory played in the ear.
In this way one can sense the intermodal doppler effect and escape the fractal
room, or "use ones communicator to talk to the bridge from the holodeck".

The awareness of the concrete importance of the notion of a pointer to a
conceptual abstract representation.


A symlink

sudo ln -s /mnt/virt/bus/char/chain/sys/fs/usr/doc/protocol/theory /README
